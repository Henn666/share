# Role & Objective
You are an expert EDA/SoC STA Automation Engineer. 
A critical numerical truncation bug exists in the current PrimeTime STA JSONL parser: the accumulated data path delay is missing approximately 66ns compared to the ground truth `data arrival time`.

# Bug Analysis & Ground Truth

1. MISSING DELAY (TRUNCATION BUG):
   - Current JSON output sum of all segments: ~4324.37ns
   - STA Report ground truth (`data arrival time`): 4390.367676ns
   - Discrepancy: Exactly ~66.00ns missing in the destination block.
   - Root Cause:
     In the report, the signal traverses into the destination subsystem wrapper:
     `u_vadsys_ext_par_wrap/u_vadsys_top_par_wrap/...`
     The segment for `u_vadsys` was incorrectly emitted with `delay: 0.0`. 
     The stage delays between crossing the boundary and reaching `.../reg_dcm_resource_mask_reg_15_/D` were skipped or flushed prematurely during region boundary transitions.

2. FRAGMENTATION & PIN NAME FIXES:
   - Clean up `pn` (Peak Instance Name): Strip the trailing pin name (the part after the last `/`), keeping only the instance name for downstream signature clustering.
   - Consolidate continuous sub-stages: Do not break contiguous routing into micro-top segments unless it genuinely leaves the subsystem wrapper back to top-level pins.
   - The total sum of `delay` across all elements in `seg` MUST match `data arrival time` within 0.001ns tolerance.

# Schema & Requirements
Maintain the defined JSONL schema while removing the redundant `mpt` field:
- Schema fields:
  * `le`: [start_line, end_line]
  * `at`: Archetype string (e.g., "MAX_DELAY_ONLY")
  * `lc`: Launch clock or "NA"
  * `cc`: Capture clock
  * `sr`: Skew-to-period ratio (float)
  * `ex`: External delay ratio or null
  * `sk`: Slack value (float)
  * `n50`: Global Top-50% stage count (integer)
  * `ns`: Total stages in data path (integer)
  * `seg`: List of tuples: `[region, delay, pt, pd, pn, th]`
    - `region`: Subsystem wrapper name or 'top'
    - `delay`: Summed Incr over this contiguous block
    - `pt`: 'c' or 'n'
    - `pd`: Peak single-stage delay within this segment
    - `pn`: Instance name of the worst stage (without pin name)
    - `th`: Minimum stages covering 50% of this segment's delay

# Instruction
Rewrite the Python parsing logic (specifically the line iteration and region-grouping state machine) to guarantee that:
1. Every row with an `Incr` value from `Startpoint` down to `data arrival time` is accounted for without omission.
2. The final destination subsystem accumulates the remaining ~66ns properly.
3. Return the fully corrected, functional Python code.
